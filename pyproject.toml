[project]
name = "nexus-email-sentinel"
version = "0.1.0"
description = "OpenEnv email triage environment for hackathon"
authors = [
  { name = "Shashank Padmasale" }
]
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic"
]

[tool.openenv]
entrypoint = "server.py"